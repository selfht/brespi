import { describe, expect, it } from "bun:test";

describe("REGRESSION > database", () => {
  it("example database test 1", () => {
    expect(1 + 1).toEqual(2);
  });
  it("example database test 2", () => {
    expect(1 + 1).toEqual(2);
  });
});
