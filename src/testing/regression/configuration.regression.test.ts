import { describe, expect, it } from "bun:test";

describe("REGRESSION > configuration", () => {
  it("example configuration test 1", () => {
    expect(1 + 1).toEqual(2);
  });
  it("example configuration test 2", () => {
    expect(1 + 1).toEqual(2);
  });
});
